{
  "name": "project-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Project CRM monorepo",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install:packages": "npm install --workspaces"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  }
}
